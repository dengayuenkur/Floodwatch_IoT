# Flood Monitoring System — Python dependencies
# Install with:  pip install -r requirements.txt

# Web framework
Flask==3.0.3
Flask-Cors==4.0.1
Flask-SocketIO==5.3.6

# Database ORM
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36

# Authentication
Flask-JWT-Extended==4.6.0

# Rate limiting
Flask-Limiter==3.7.0

# Password hashing (used by Werkzeug, already bundled with Flask)
# Werkzeug is installed as a Flask dependency

# Production WSGI server (used on Render / any non-dev deployment)
# Flask-SocketIO runs in async_mode="threading", so gunicorn's default
# gthread worker is used in production — no eventlet/gevent needed.
gunicorn==23.0.0

# SMS alerts (optional — comment out if Twilio is not used)
twilio==9.2.3

# Environment variable loading
python-dotenv==1.0.1

# MQTT client (optional — only needed if using MQTT alongside HTTP)
paho-mqtt==2.1.0
